# Notas:

```
Opciones:
      --help     Muestra ayuda                                        [booleano]
      --version  Muestra número de versión                            [booleano]
  -b, --base     Es la base de tabla de multiplicar         [número] [requerido]
  -l, --Listado  muestra la tabla en consola         [booleano] [defecto: false]
  -h, --hasta    Es el exponente de la tabla hasta donde quieres
                                                          [número] [defecto: 10]
```